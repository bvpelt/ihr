--
--  manytomany for plan - overheid
--
create table plan_overheid
(
    id          bigint not null primary key,
    plan_id     bigint,
    overheid_id bigint
);

create index plan_overheid_gebiedsaanwijzing_idx on plan_overheid (plan_id);
create index plan_overheid_artikel_idx on plan_overheid (overheid_id);

ALTER TABLE public.plan_overheid
    OWNER TO testuser;

ALTER TABLE public.plan_overheid
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.plan_overheid_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );

--
--  overheid
--
create table overheid
(
    id   bigint not null primary key,
    type varchar(24),
    code varchar(4),
    naam text
);

create index overheid_type_idx on overheid (type);
create index overheid_code_idx on overheid (code);


ALTER TABLE public.overheid
    OWNER TO testuser;

ALTER TABLE public.overheid
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.overheid_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );

--
--  locatienaam
--
create table locatienaam
(
    id   bigint not null primary key,
    naam text
);

create index overheid_naam_idx on locatienaam (naam);

ALTER TABLE public.locatienaam
    OWNER TO testuser;

ALTER TABLE public.locatienaam
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.locatienaam_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );


--
--  manytomany for plan - locatienaam
--
create table plan_locatienaam
(
    id             bigint not null primary key,
    plan_id        bigint,
    locatienaam_id bigint
);

create index plan_locatienaam_plan_idx on plan_locatienaam (plan_id);
create index plan_locatienaam_locatienaam_idx on plan_locatienaam (locatienaam_id);

ALTER TABLE public.plan_locatienaam
    OWNER TO testuser;

ALTER TABLE public.plan_locatienaam
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.plan_locatienaam_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );

