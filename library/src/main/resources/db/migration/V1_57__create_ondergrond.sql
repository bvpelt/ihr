-- table ondergrond
create table ondergrond
(
    id   bigint not null primary key,
    type text,
    datum text
);

create index ondergrond_norm_idx on ondergrond (type);

ALTER TABLE ondergrond
    OWNER TO testuser;

ALTER TABLE ondergrond
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME ondergrond_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
