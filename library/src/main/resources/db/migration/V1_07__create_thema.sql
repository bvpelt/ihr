-- table thema
create table thema
(
    id    bigint not null primary key,
    thema text
);

create unique index thema_thema_idx on thema (thema);

ALTER TABLE thema
    OWNER TO testuser;

ALTER TABLE thema
ALTER
COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME thema_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
