-- table kruimelpad
create table kruimelpad
(
    id            bigint not null primary key,
    identificatie text,
    titel         text,
    volgnummer    int,
    tekst_id      bigint
);

create index kruimelpad_identificatie_idx on kruimelpad (identificatie);
create unique index kruimelpad_identificatie_titel_volgnummer_idx on kruimelpad (identificatie, titel, volgnummer);


ALTER TABLE kruimelpad
    OWNER TO testuser;

ALTER TABLE kruimelpad
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME kruimelpad_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
