
--
--  tekstref
--
create table tekstref
(
    id                      bigint not null primary key,
    referentie              text,
    structuurvisiegebied_id bigint,
    gebiedsaanduiding_id    bigint
);

create index tekstref_structuurvisiegebied_idx on tekstref (structuurvisiegebied_id);
create index tekstref_gebiedsaanduiding_idx on tekstref (gebiedsaanduiding_id);

ALTER TABLE public.tekstref
    OWNER TO testuser;

ALTER TABLE public.tekstref
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.tekstref_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
