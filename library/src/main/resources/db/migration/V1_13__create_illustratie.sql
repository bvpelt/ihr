-- table illustratie
create table illustratie
(
    id          bigint not null primary key,
    href        text,
    type        text,
    naam        text,
    legendanaam text
);

create index illustratie_type_idx on illustratie (type);
create index illustratie_naam_idx on illustratie (naam);

ALTER TABLE illustratie
    OWNER TO testuser;

ALTER TABLE illustratie
ALTER
COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME illustratie_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
