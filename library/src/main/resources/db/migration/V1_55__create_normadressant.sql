-- table normadressant
create table normadressant
(
    id   bigint not null primary key,
    norm text
);

create unique index normadressant_norm_idx on normadressant (norm);

ALTER TABLE normadressant
    OWNER TO testuser;

ALTER TABLE normadressant
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME normadressant_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
