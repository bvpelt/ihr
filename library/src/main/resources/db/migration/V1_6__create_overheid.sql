create table overheid
(
    id            bigint not null primary key,
    type          text,
    code          text,
    beleidsmatig boolean default false,
    publicerend boolean default false,
    plan_identificatie text   not null
);

create unique index overheid_plan_identificatie_code_ix on overheid (plan_identificatie, code);
create index overheid_code_ix on overheid (code);
create index overheid_plan_identificatie_ix on overheid (plan_identificatie);

ALTER TABLE public.overheid
    OWNER TO testuser;

ALTER TABLE public.overheid
ALTER
COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.overheid_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );