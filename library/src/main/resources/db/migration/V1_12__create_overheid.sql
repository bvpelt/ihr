-- table overheid
create table overheid
(
    id   bigint not null primary key,
    type varchar(24),
    code varchar(4),
    naam text
);

create index overheid_type_idx on overheid (type);
create unique index overheid_code_idx on overheid (code);


ALTER TABLE overheid
    OWNER TO testuser;

ALTER TABLE overheid
    ALTER
        COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME overheid_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );
